namespace LocalEdit.Shared
{
    public class Utils
    {
        public static string VOD(string? originalValue)
        {
            return (originalValue != null) ? originalValue : "";
        }
    }
}
